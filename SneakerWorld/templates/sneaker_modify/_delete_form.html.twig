
<div class="d-flex justify-content-center">
    <div class="card" style="width: 18rem;">
        <div class="card-body">
            <form method="post" class="form-group"action="{{ path('sneaker_modify_delete', {'id': sneaker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sneaker.id) }}">
                <button class="btn btn-danger">{{ 'sneaker.supprimer'|trans }}</button>
            </form>
        </div>
    </div>
</div>
