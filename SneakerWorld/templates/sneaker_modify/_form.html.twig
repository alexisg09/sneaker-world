{% extends 'base.html.twig' %}

{% block body %}
    {% include './/common/headeradmin.html.twig' %}
    <div class="d-flex justify-content-center">
        <div class="card" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">{{ 'button.modifsneaker'|trans }}</h5>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-primary">{{ 'button.modifier'|trans|default('Save') }}</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
